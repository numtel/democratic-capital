{
  "name": "democratic-capital",
  "version": "1.0.0",
  "description": "Democratically controlled cryptocurrency",
  "scripts": {
    "dev": "lite-server",
    "dev-chain": "node dev-chain.js",
    "test": "node test/index.js",
    "todo": "bash -c \"grep -rn 'TODO' --exclude-dir={node_modules,build} --exclude={*.min*,*.swp,package.json,solc} *\"",
    "build-mock": "./solc --bin --abi --overwrite -o build contracts/MockVerification.sol",
    "build-dev": "./solc --bin --abi --overwrite -o build contracts/DemocraticToken.sol",
    "build-prod": "./solc --bin --abi --overwrite -o build --optimize contracts/DemocraticToken.sol"
  },
  "author": "numtel <ben@latenightsketches.com>",
  "license": "MIT",
  "devDependencies": {
    "ganache": "^7.0.4",
    "lite-server": "^2.3.0"
  },
  "dependencies": {
    "web3": "^1.7.1"
  }
}
