{
  "name": "democratic-capital",
  "version": "1.0.0",
  "description": "Democratically controlled cryptocurrency",
  "source": "src/index.html",
  "staticFiles": {
    "staticPath": "build"
  },
  "scripts": {
    "dev": "parcel --port 3000 --open",
    "build": "parcel build",
    "dev-chain": "node dev-chain.js",
    "test": "node test/index.js",
    "todo": "bash -c \"grep -rn 'TODO' --exclude-dir={node_modules,build,dist} --exclude={*.min*,*.swp,package.json,solc} *\"",
    "build-dev": "./solc --bin --abi --overwrite -o build contracts/*.sol",
    "build-prod": "./solc --bin --abi --overwrite -o build --optimize contracts/*.sol"
  },
  "author": "numtel <ben@latenightsketches.com>",
  "license": "MIT",
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.6.0",
    "@parcel/transformer-webmanifest": "^2.6.0",
    "assert": "^2.0.0",
    "ganache": "^7.0.4",
    "https-browserify": "^1.0.0",
    "os-browserify": "^0.3.0",
    "parcel": "^2.6.0",
    "parcel-reporter-static-files-copy": "^1.3.4",
    "querystring-es3": "^0.2.1",
    "stream-http": "^3.2.0",
    "url": "^0.11.0"
  },
  "dependencies": {
    "@coinbase/wallet-sdk": "^3.2.0",
    "abi-decoder": "^2.4.0",
    "lit": "^2.2.5",
    "web3": "^1.7.3",
    "web3modal": "^1.9.7"
  }
}
